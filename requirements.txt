# --- Sprint 1 ---
gradio
pandas
python-dotenv
faster-whisper

# --- Sprint 2 ---
gspread
oauth2client
spacy==3.1.0

# 🚩 CORRECCIÓN CLAVE: Fija Pydantic a una versión V1
# La versión 1.10.13 es compatible con spacy 3.1 y resuelve el error 'ModelMetaclass'.
pydantic==1.10.13

# Descarga el modelo de lenguaje de spaCy por separado (no es un paquete PyPI)
# La versión del modelo debe coincidir con la de spaCy (es-core-news-sm==3.1.0)
# Esto se instala en el Dockerfile o se descarga mediante código en app.py.

# --- Sprint 3 ---
scikit-learn
joblib